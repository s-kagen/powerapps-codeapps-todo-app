/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 * This file is autogenerated. Do not edit this file directly.
 */

export type ToDocontentType = 'Text'|'HTML';
export type ToDoimportance = 'Low'|'Normal'|'High';
export type ToDostatus = 'NotStarted'|'InProgress'|'Completed'|'WaitingOnOthers'|'Deferred';

export interface ToDo {
  "@odata.context"?: string;
  "@odata.id"?: string;
  "@odata.etag"?: string;
  // Unique identifier of the to-do.
  id?: string;
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  createdDateTime?: string;
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  lastModifiedDateTime?: string;
  changeKey?: string;
  // Collection of category names associated with the to-do.
  categories?: string[];
  // Name of the person who has been assigned the to-do.
  assignedTo?: string;
  // To-do body that typically contains information about the to-do.
  body?: {
    // Text=0, HTML=1
    contentType?: ToDocontentType;
    // The text or HTML content.
    content?: string;
  };
  // Date-time in the UTC time zone when the to-do was finished.
  completedDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Date in the UTC time zone when the to-do is to be finished.
  dueDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Low, normal or high.
  importance?: ToDoimportance;
  // True if an alert is set to remind the user of the to-do.
  isReminderOn?: boolean;
  // Name of the person who created the to-do.
  owner?: string;
  // Unique identifier of the parent folder.
  parentFolderId?: string;
  // Date-time in the UTC time zone for a reminder alert of the to-do to occur.
  reminderDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Date in the UTC time zone when the to-do is to begin.
  startDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  status?: ToDostatus;
  // Brief description or title of the to-do.
  subject?: string;
}

export type ToDoHtmlimportance = 'Low'|'Normal'|'High';
export type ToDoHtmlstatus = 'NotStarted'|'InProgress'|'Completed'|'WaitingOnOthers'|'Deferred';

export interface ToDoHtml {
  "@odata.context"?: string;
  "@odata.id"?: string;
  "@odata.etag"?: string;
  // Unique identifier of the to-do.
  id?: string;
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  createdDateTime?: string;
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  lastModifiedDateTime?: string;
  // Collection of category names associated with the to-do.
  categories?: string[];
  // Name of the person who has been assigned the to-do.
  assignedTo?: string;
  // To-do body that typically contains information about the to-do.
  body?: {
    // The content.
    content?: string;
  };
  // Date-time in the UTC time zone when the to-do was finished.
  completedDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Date in the UTC time zone when the to-do is to be finished.
  dueDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Low, normal or high.
  importance?: ToDoHtmlimportance;
  // True if an alert is set to remind the user of the to-do.
  isReminderOn?: boolean;
  // Name of the person who created the to-do.
  owner?: string;
  // Unique identifier of the parent folder.
  parentFolderId?: string;
  // Date-time in the UTC time zone for a reminder alert of the to-do to occur.
  reminderDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Date in the UTC time zone when the to-do is to begin.
  startDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    dateTime?: string;
  };
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  status?: ToDoHtmlstatus;
  // Brief description or title of the to-do.
  subject?: string;
}

export type CreateToDoBodyImportance = 'Low'|'Normal'|'High';
export type CreateToDoBodyStatus = 'NotStarted'|'InProgress'|'Completed'|'WaitingOnOthers'|'Deferred';
export type CreateToDoBodySensitivity = 'Normal'|'Personal'|'Private'|'Confidential';
export type CreateToDoBodyContentType = 'Text'|'HTML';

export interface CreateToDoBody {
  // Date in the UTC time zone when the to-do is to be finished.
  DueDateTime?: {
    // YYYY-MM-DD or YYYY-MM-DDThh:mm:ssZ (note the time portion will be ignored)
    DateTime?: string;
    // Time zone of the due date.
    TimeZone: string;
  };
  // Date-time in UTC time zone for a reminder alert of the to-do to occur.
  ReminderDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    DateTime?: string;
    // Time zone of the reminder date.
    TimeZone: string;
  };
  // Date in the UTC time zone when the to-do is to begin.
  StartDateTime?: {
    // YYYY-MM-DD or YYYY-MM-DDThh:mm:ssZ (note the time portion will be ignored)
    DateTime?: string;
    // Time zone of the start date.
    TimeZone: string;
  };
  // Low, normal or high.
  Importance?: CreateToDoBodyImportance;
  // Brief description or title of the to-do.
  Subject: string;
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  Status?: CreateToDoBodyStatus;
  // Indicates the level of privacy for the event.
  Sensitivity?: CreateToDoBodySensitivity;
  // To-do body that typically contains information about the to-do.
  Body?: {
    // Text=0, HTML=1
    ContentType?: CreateToDoBodyContentType;
    // The text or HTML content.
    Content?: string;
  };
  // Collection of category names associated with the to-do.
  Categories?: string[];
  // True if an alert is set to remind the user of the to-do.
  IsReminderOn?: boolean;
}

export type CreateToDoHtmlBodyImportance = 'Low'|'Normal'|'High';
export type CreateToDoHtmlBodyStatus = 'NotStarted'|'InProgress'|'Completed'|'WaitingOnOthers'|'Deferred';
export type CreateToDoHtmlBodySensitivity = 'Normal'|'Personal'|'Private'|'Confidential';

export interface CreateToDoHtmlBody {
  // Date in the UTC time zone when the to-do is to be finished.
  DueDateTime?: {
    // YYYY-MM-DD or YYYY-MM-DDThh:mm:ssZ (note the time portion will be ignored)
    DateTime?: string;
    // Time zone of the due date.
    TimeZone: string;
  };
  // Date-time in UTC time zone for a reminder alert of the to-do to occur.
  ReminderDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    DateTime?: string;
    // Time zone of the reminder date.
    TimeZone: string;
  };
  // Date in the UTC time zone when the to-do is to begin.
  StartDateTime?: {
    // YYYY-MM-DD or YYYY-MM-DDThh:mm:ssZ (note the time portion will be ignored)
    DateTime?: string;
    // Time zone of the start date.
    TimeZone: string;
  };
  // Low, normal or high.
  Importance?: CreateToDoHtmlBodyImportance;
  // Brief description or title of the to-do.
  Subject: string;
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  Status?: CreateToDoHtmlBodyStatus;
  // Indicates the level of privacy for the event.
  Sensitivity?: CreateToDoHtmlBodySensitivity;
  // To-do body that typically contains information about the to-do.
  Body?: {
    // HTML
    ContentType: string;
    // The content.
    Content?: string;
  };
  // Collection of category names associated with the to-do.
  Categories?: string[];
  // True if an alert is set to remind the user of the to-do.
  IsReminderOn?: boolean;
}

export type UpdateToDoBodyImportance = 'Low'|'Normal'|'High';
export type UpdateToDoBodyStatus = 'NotStarted'|'InProgress'|'Completed'|'WaitingOnOthers'|'Deferred';
export type UpdateToDoBodySensitivity = 'Normal'|'Personal'|'Private'|'Confidential';
export type UpdateToDoBodyContentType = 'Text'|'HTML';

export interface UpdateToDoBody {
  // Date in the UTC time zone when the to-do is to be finished.
  DueDateTime?: {
    // YYYY-MM-DD or YYYY-MM-DDThh:mm:ssZ (note the time portion will be ignored)
    DateTime?: string;
    // Time zone of the due date.
    TimeZone: string;
  };
  // Date-time in UTC time zone for a reminder alert of the to-do to occur.
  ReminderDateTime?: {
    // YYYY-MM-DDThh:mm:ssZ (UTC format)
    DateTime?: string;
    // Time zone of the reminder date.
    TimeZone: string;
  };
  // Date in the UTC time zone when the to-do is to begin.
  StartDateTime?: {
    // YYYY-MM-DD or YYYY-MM-DDThh:mm:ssZ (note the time portion will be ignored)
    DateTime?: string;
    // Time zone of the start date.
    TimeZone: string;
  };
  // Low, normal or high.
  Importance?: UpdateToDoBodyImportance;
  // Brief description or title of the to-do.
  Subject?: string;
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  Status?: UpdateToDoBodyStatus;
  // Indicates the level of privacy for the event.
  Sensitivity?: UpdateToDoBodySensitivity;
  // To-do body that typically contains information about the to-do.
  Body?: {
    // Text=0, HTML=1
    ContentType?: UpdateToDoBodyContentType;
    // The text or HTML content.
    Content?: string;
  };
  // Collection of category names associated with the to-do.
  Categories?: string[];
  // True if an alert is set to remind the user of the to-do.
  IsReminderOn?: boolean;
}

export interface TodoList {
  "@odata.id"?: string;
  // Unique identifier of the to-do list.
  id?: string;
  changeKey?: string;
  // Name of the to-do list.
  name?: string;
  // True if this is the default to-do list.
  isDefaultFolder?: boolean;
  parentGroupKey?: string;
}

export interface TodoList_V2 {
  "@odata.etag"?: string;
  // Unique identifier of the to-do list.
  id?: string;
  // Name of the to-do list.
  displayName?: string;
  // Property indicating the well-known list name if the given list is a well-known list. Possible values are: none, defaultList, flaggedEmails, unknownFutureValue.
  wellknownListName?: string;
  // True if the user is owner of the given task list.
  isOwner?: boolean;
  // True if the task list is shared with other users.
  isShared?: boolean;
}

export interface CreateToDoListBody {
  // List name
  name: string;
}

export type ToDo_V2importance = 'low'|'normal'|'high';
export type ToDo_V2status = 'notStarted'|'inProgress'|'completed'|'waitingOnOthers'|'deferred';

export interface ToDo_V2 {
  "@odata.context"?: string;
  "@odata.etag"?: string;
  // Unique identifier of the to-do.
  id?: string;
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  createdDateTime?: string;
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  lastModifiedDateTime?: string;
  // To-do body that typically contains information about the to-do.
  body?: {
    // The type of the content. Possible values are text and html.
    contentType?: string;
    // The content of the item.
    content?: string;
  };
  // YYYY-MM-DDThh:mm:ssZ (UTC format)
  bodyLastModifiedDateTime?: string;
  // The date in the specified time zone that the task was finished.
  completedDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
  };
  // The date in the specified time zone that the task is to be finished.
  dueDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
  };
  // Low, normal or high.
  importance?: ToDo_V2importance;
  // True if an alert is set to remind the user of the to-do.
  isReminderOn?: boolean;
  // The date and time for a reminder alert of the task to occur.
  reminderDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
  };
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  status?: ToDo_V2status;
  // Brief description or title of the to-do.
  title?: string;
}

export type CreateToDo_V2importance = 'low'|'normal'|'high';
export type CreateToDo_V2status = 'notStarted'|'inProgress'|'completed'|'waitingOnOthers'|'deferred';

export interface CreateToDo_V2 {
  // Date in the UTC time zone when the to-do is to be finished (note the time portion will be ignored).
  dueDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
    // Time zone of the due date.
    timeZone: string;
  };
  // Date-time in UTC time zone for a reminder alert of the to-do to occur.
  reminderDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
    // Time zone of the reminder date.
    timeZone: string;
  };
  // Low, normal or high.
  importance?: CreateToDo_V2importance;
  // Brief description or title of the to-do.
  title: string;
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  status?: CreateToDo_V2status;
  // To-do body that typically contains information about the to-do.
  body?: {
    // The type of the content. Possible values are text and html.
    contentType: string;
    // The content of the item.
    content?: string;
  };
  // True if an alert is set to remind the user of the to-do.
  isReminderOn?: boolean;
}

export type UpdateToDo_V2importance = 'low'|'normal'|'high';
export type UpdateToDo_V2status = 'notStarted'|'inProgress'|'completed'|'waitingOnOthers'|'deferred';

export interface UpdateToDo_V2 {
  // Date in the UTC time zone when the to-do is to be finished (note the time portion will be ignored).
  dueDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
    // Time zone of the due date.
    timeZone: string;
  };
  // Date-time in UTC time zone for a reminder alert of the to-do to occur.
  reminderDateTime?: {
    // YYYY-MM-DDThh:mm:ss
    dateTime?: string;
    // Time zone of the reminder date.
    timeZone: string;
  };
  // Low, normal or high.
  importance?: UpdateToDo_V2importance;
  // Brief description or title of the to-do.
  title?: string;
  // Indicates state or progress of the to-do - not started, in progress, completed, waiting on others or deferred.
  status?: UpdateToDo_V2status;
  // To-do body that typically contains information about the to-do.
  body?: {
    // The type of the content. Possible values are text and html.
    contentType: string;
    // The content of the item.
    content?: string;
  };
  // True if an alert is set to remind the user of the to-do.
  isReminderOn?: boolean;
}

export interface CreateToDoList_V2 {
  // List name
  displayName: string;
}
